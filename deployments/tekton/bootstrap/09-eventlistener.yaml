# Generated by RepoSentry
# File: 08-eventlistener.yaml
---
apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: reposentry-standard-eventlistener
  namespace: reposentry-system
  labels:
    reposentry.io/component: "bootstrap"
    reposentry.io/type: "eventlistener"
spec:
  serviceAccountName: reposentry-bootstrap-sa
  triggers:
  - name: reposentry-tekton-detected
    # CEL filter disabled - network isolation of webhook endpoint provides sufficient security in production
    # To enable filtering, uncomment the following configuration:
    # interceptors:
    # - name: "reposentry-filter"
    #   ref:
    #     name: "cel"
    #   params:
    #   - name: "filter"
    #     value: "header.get('ce-source', '').startsWith('reposentry')"
    bindings:
    - ref: reposentry-bootstrap-binding
    template:
      ref: reposentry-bootstrap-template
---
