# Generated by RepoSentry
# File: 10-ingress.yaml
# 
# Configuration Variables (customize before deployment):
# - INGRESS_CLASS: Ingress controller class name (default: nginx)
# - WEBHOOK_HOST: Webhook domain (default: webhook.10.78.14.61.nip.io)
# - SSL_REDIRECT: Enable SSL redirect (default: false)
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reposentry-eventlistener-ingress
  namespace: reposentry-system
  labels:
    reposentry.io/component: "bootstrap"
    reposentry.io/type: "ingress"
  annotations:
    # Ingress Controller specific annotations (customize as needed)
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    # For other controllers, you might need different annotations:
    # traefik.ingress.kubernetes.io/rewrite-target: /
    # kubernetes.io/ingress.class: "traefik"  # for older Traefik versions
spec:
  # CUSTOMIZE: Change this based on your Ingress Controller
  # Common values: nginx, traefik, haproxy, istio, etc.
  ingressClassName: nginx
  rules:
  - host: webhook.10.78.14.61.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: el-reposentry-standard-eventlistener
            port:
              number: 8080
---
